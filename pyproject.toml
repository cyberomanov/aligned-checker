[project]
name = "aligned-checker"
version = "1.0"
description = ""
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "beautifulsoup4>=4.13,<5",
    "curl-cffi>=0.15,<1",
    "eth-account>=0.13,<0.14",
]
